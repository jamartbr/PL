int main {
    local {
        const int id1 = -98;
        const char a = 'a';
        const list char b = ['b'];
        bool c,b1;
        list int l1,l2,l3,l4;
        char a1,a2,a3;
        int i2, A;
    }
    
    int suma1(int entero, int long) {
        local {
            int a, b, c, d;  
        }

        return {a}+{b};
    };
    
    bool estaDentro(list lista, int long) {
        return ({long} < #{lista});
    };
    
    b1 = {l1} + #{l1};

    

    return 0;

}
